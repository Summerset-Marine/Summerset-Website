# PROMPT 3.5 — PATCH: REMOVE GMAIL DEPENDENCIES, ADD HUBSPOT AND COOKIE-PARSER
# Summerset Marine Construction — LaBonte Development
# Run this prompt after Prompt 3 is confirmed complete, before running Prompt 4

---

Prompts 1–3 were built with Gmail OAuth2 as the form submission method. The project has since switched to HubSpot Forms API. This prompt patches the existing project to remove Gmail-related code and dependencies and put the correct foundation in place before Prompt 4 runs.

Complete these changes in exact order and confirm each one before moving to the next.

---

STEP 1 — UNINSTALL GMAIL PACKAGES:

Run this command in the Replit shell:

```bash
npm uninstall googleapis nodemailer
```

Confirm the packages are removed from node_modules and package.json before continuing.

---

STEP 2 — INSTALL HUBSPOT AND COOKIE-PARSER:

```bash
npm install @hubspot/api-client cookie-parser
```

Confirm both packages appear in package.json dependencies before continuing.

---

STEP 3 — DELETE src/lib/gmail.js:

Delete the file src/lib/gmail.js entirely. It will be replaced by src/lib/hubspot.js in Prompt 4.

---

STEP 4 — UPDATE package.json DESCRIPTION LINE (if present):

If the package.json contains any reference to "gmail" or "oauth2" in its description or scripts fields, remove it.

---

STEP 5 — UPDATE .env.example:

Remove these lines from .env.example:

```
# Gmail OAuth2 (Google Service Account)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_SENDER_ADDRESS=
FORM_RECIPIENT_EMAIL=
```

Replace them with:

```
# HubSpot Forms API
HUBSPOT_ACCESS_TOKEN=
HUBSPOT_PORTAL_ID=
VITE_HUBSPOT_PORTAL_ID=
HUBSPOT_FORM_ID_CONSULTATION=
HUBSPOT_FORM_ID_CONTACT=
HUBSPOT_FORM_ID_SERVICE_REQUEST=
```

Do not change any other lines in .env.example.

---

STEP 6 — UPDATE server.js:

The current server.js does not import or use cookie-parser. Add it now.

Find this block at the top of server.js:

```javascript
import express from 'express'
import path from 'path'
import { fileURLToPath } from 'url'
```

Replace with:

```javascript
import express from 'express'
import cookieParser from 'cookie-parser'
import path from 'path'
import { fileURLToPath } from 'url'
```

Then find this line:

```javascript
app.use(express.json())
```

Add cookie-parser immediately after it:

```javascript
app.use(express.json())
app.use(cookieParser())
```

Do not change any other lines in server.js.

---

STEP 7 — UPDATE src/api/submit-form.js:

The current submit-form.js imports from gmail.js which no longer exists. Update the import line so the file does not crash on startup.

Find the top of src/api/submit-form.js. It will contain an import referencing gmail. Replace the entire top import block with a placeholder comment so the file does not throw a module-not-found error:

```javascript
// HubSpot submission handlers will be imported here in Prompt 4
// This file will be fully replaced in Prompt 4

export default async function submitForm(req, res) {
  res.status(503).json({ message: 'Form handler not yet configured — Prompt 4 pending' })
}
```

This is a temporary stub. Prompt 4 will replace this entire file.

---

STEP 8 — VERIFY BUILD:

Run:

```bash
npm run build
```

The build must complete without errors before Prompt 4 is run. If there are any import errors referencing gmail, googleapis, or nodemailer, locate and remove those imports before proceeding.

Report the result of the build check. Confirm all 7 steps above are complete before marking this prompt done.